import Foundation

public extension WebView {
    struct Props {
        public let url: String

        public init(
                url: String
        ) {
            self.url = url
        }
    }
}
